name: Swift

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Swift version
  # You may pin to the exact commit or the version.
  # uses: sersoft-gmbh/swift-version-action@f05fc577db170ebe6991af280f060227d21d1bb0
      uses: sersoft-gmbh/swift-version-action@v1.0.0
